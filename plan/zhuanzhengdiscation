了解协议报文，pboss在整个框架中处于什么位置
熟悉接口
熟悉服开
ec白名单配置页面
变动后的数据需要与库中的数据进行校验，必须与库中有联系的数据的某些字段相同，并不只是检查唯一性
没有主键，使用三个字段做唯一索引，进行更新操作需要对三个字段是否变化的情况分开执行逻辑。
遇到的主要问题：ie的兼容性，
尽量使用框架插件控制页面的状态变化，如果一定要手动增加控制，要用原生的css
ajax后台不能返回空值，ie7对空值的处理与ie其它版本不一致。
未来
将各个部分的代码整理清楚，理解模块间的联系，在日后的开发中做的更好。
高质完成自己的工作后，参与工作链条中的其它环节
在能力范围内帮助他人，营造更好的工作氛围。
